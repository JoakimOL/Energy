newtype coordinate = (int x, int y)
newtype test = (coordinate x, int y)

main() -> int
main() = {
    int x = 100;
    coordinate coor = new_coordinate(2,x);
    // coor.x = 3;
    printf("%d", coor.x);
    printf("%d", coor.y);

    // test woot = new_test(coor, 5);
    // printf("%d", woot.y);

    return 3;
}

